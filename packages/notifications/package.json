{
  "name": "@wellos/notifications",
  "private": true,
  "version": "0.0.0",
  "description": "Email/SMS provider interfaces and adapters for Wellos (Postmark, TextLink)",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "echo 'lint placeholder' && exit 0",
    "typecheck": "tsc --noEmit",
    "test": "echo 'test placeholder' && exit 0"
  },
  "devDependencies": {
    "typescript": "5.6.3"
  }
}
