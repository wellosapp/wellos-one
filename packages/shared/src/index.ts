// Placeholder. Shared types, utils, and schema constants will live here.
// Avoid putting domain logic in this package — keep it for code that BOTH
// apps/api AND the frontend apps need to reference.
export const PACKAGE_NAME = '@wellos/shared';
