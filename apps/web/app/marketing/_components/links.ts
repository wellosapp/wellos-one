// Centralized URLs used across the marketing site so a change to the app
// host doesn't fan out across every section component.

export const APP_ORIGIN = 'https://app.wellos.one';
export const SIGN_IN_URL = `${APP_ORIGIN}/sign-in`;
export const SIGN_UP_URL = `${APP_ORIGIN}/sign-up`;
