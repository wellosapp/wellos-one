{
  "name": "@wellos/shared",
  "private": true,
  "version": "0.0.0",
  "description": "Shared types, utilities, and schema constants used by both apps/api and the frontend apps",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "echo 'lint placeholder' && exit 0",
    "typecheck": "tsc --noEmit",
    "test": "echo 'test placeholder' && exit 0"
  },
  "devDependencies": {
    "typescript": "5.6.3"
  }
}
